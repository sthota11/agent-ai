const form = document.getElementById("userForm"); const loading = document.getElementById("loading"); const successMessage = document.getElementById("successMessage"); form.addEventListener("submit", function(event) { event.preventDefault(); loading.style.display = "block"; successMessage.style.display = "none"; // mimic server delay setTimeout(() => { loading.style.display = "none"; successMessage.style.display = "block"; }, 2000); }); form.addEventListener("reset", function() { successMessage.style.display = "none"; loading.style.display = "none"; });
